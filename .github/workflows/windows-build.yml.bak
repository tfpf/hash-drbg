name: windows-build
on: [push, workflow_dispatch]
defaults:
  run:
    shell: bash

jobs:
  c:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - run: ./run.sh
      - run: cd tests && make && ./tests
